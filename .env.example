# Base URL the Axios client (src/services/api.ts) sends requests to.
# In dev, requests to `/api/*` are proxied to the local backend by
# vite.config.ts's server.proxy — leave this as `/api` unless the backend
# isn't reachable at that path (see vite.config.ts's proxy target).
VITE_API_BASE_URL=/api
